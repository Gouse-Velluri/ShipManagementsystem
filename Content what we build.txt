What's built:
PageFeaturesLoginEmail/password auth, demo credential autofill, session persistenceDashboardKPI cards, donut chart (jobs by status), bar chart (by priority), fleet + jobs tablesShipsFull CRUD, search, status filter, links to detailShip DetailGeneral info, stats, components tab, full maintenance historyComponentsAll components across fleet, overdue detection (>365 days), filter by ship/conditionJobsCreate/edit/delete, filter by ship+status+priority, one-click status advance, auto-updates component's last maintenance dateCalendarMonthly + weekly view, clickable dates show job panel, color-coded by status